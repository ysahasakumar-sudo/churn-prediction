# Requirements for the Churn Prediction notebook
# Tested with Python 3.10+ (developed on 3.12)
#
# Install with:
#   pip install -r requirements.txt
#
# Version ranges are intentionally loose so the notebook keeps running on
# newer minor releases; pin exactly (change >= to ==) if you need a
# byte-for-byte reproducible environment.

# Data handling
pandas>=2.0
numpy>=1.24

# Modelling
scikit-learn>=1.3
xgboost>=2.0

# Plotting
matplotlib>=3.7
seaborn>=0.12

# Model persistence (for the viva demo)
joblib>=1.3

# Notebook (only needed if launching Jupyter yourself; Colab/VS Code bring their own)
jupyter>=1.0
